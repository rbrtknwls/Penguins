window.onload = function() {

    Scenes.start(3)
        .then(function(x) {
            //Scenes.walk()
            //Scenes.skate()
            //Scenes.snow()
            Scenes.game()
        })

    
}
